import { StyleSheet } from 'react-native';

export const views = StyleSheet.create({
  thumbnailWrapper: {
    marginTop: 40,
    marginBottom: 20,
    alignItems: 'center',
  },
  thumbnailButton: {},
  thumbnail: {
    width: 130,
    height: 130,
    marginVertical: -20,
    resizeMode: 'cover',
  },
});
