import { StyleSheet } from 'react-native';

export const views = StyleSheet.create({
  thumbnailWrapper: {
    marginTop: 40,
    marginBottom: 20,
    alignItems: 'center',
  },
  thumbnailButton: {},
  thumbnail: {
    width: 100,
    height: 100,
    resizeMode: 'contain',
  },
});
